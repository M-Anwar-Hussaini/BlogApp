<div class="user-container">
  <% @users.each do |user| %>
    <div class="user-wrapper">
      <div class="photo-box">
        <img class="user-img" src="
        <% user.photo %>" alt="<%= user.name %>">
      </div>

      <div class="text-box">
        <h3><%= user.name %></h3>
        <p>Number of posts: <%= user.posts_counter %></p>
      </div>
    </div>
  <% end %>
</div>